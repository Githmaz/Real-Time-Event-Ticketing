import { CustomerDashboardData } from './customer-dashboard-data.model';

describe('CustomerDashboardData', () => {
  it('should create an instance', () => {
    expect(new CustomerDashboardData()).toBeTruthy();
  });
});
